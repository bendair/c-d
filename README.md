# Compress/Decompress String (c-d)

A simple reversible string encoder/decoder using base64 only (no third-party libs).

## Usage

    ```
    #Encode a string:
    python c-d2.py encode "your text here"
    ```
    ```
    # Decode a token:
    python c-d2.py decode "token_here"
    ```
